<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
      SKINS
    </title>

    <link rel="preconnect" href="https://fonts.bunny.net" />
    <link
      href="https://fonts.bunny.net/css?family=instrument-sans:400,400i,500,500i,600,600i,700,700i"
      rel="stylesheet"
    />

    @vite(['resources/css/app.css', 'resources/js/app.js'])
  </head>
  <body class="bg-slate-800">
    <div class="p-2 grid grid-cols-[repeat(auto-fill,minmax(200px,1fr))] gap-5">
      @each(skin in skins)
        <div class="border-2 border-gray-800 shadow-xl h-72 rounded-md bg-gray-700 hover:bg-gray-600">
          <div class="flex justify-center">
            <img src="{{ skin.imagePath }}" class="h-36" />
          </div>
          <div class="text-white text-sm text-center p-2">
            <p class="font-bold">
              R$  {{ skin.price?.toFixed(2) || '79.99' }}
            </p>
            <p class="font-thin">
              {{ skin.name }}
            </p>
          </div>
          <div class=" text-center">
            @include('components/button')
          </div>
        </div>
      @end
    </div>
  </body>
</html>
